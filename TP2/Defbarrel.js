class Plane extends CGFobject{
    constructor(scene, base,  middle, height, slices, stacks) {
        this.scene=scene;
        this.base=base;
        this.middle=middle;
        this.height=height;
        thios.slices=slices;
        this.stacks=stacks;
    }
    display(){
    }
}