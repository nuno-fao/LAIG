class Defbarrel extends CGFobject{
    constructor(scene, base,  middle, height, slices, stacks) {
        super(scene);
        this.base=base;
        this.middle=middle;
        this.height=height;
        thios.slices=slices;
        this.stacks=stacks;
    }
    display(){
    }
}